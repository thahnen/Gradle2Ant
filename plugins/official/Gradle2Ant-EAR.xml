<?xml version="1.0" encoding="UTF-8" ?>
<project name="Gradle2Ant-EAR">
    <description>
        Call Gradle from Ant - Gradle EAR plugin!
    </description>

    <!-- General properties! -->
    <dirname property="current.dir" file="${ant.file}" />

    <!-- import general Gradle2Ant.xml library -->
    <import file="../../Gradle2Ant.xml" />


    <!-- Gradle task "ear" to create enterprise archive -->
    <macrodef name="gradle.ear" description="Gradle task 'ear'">
        <attribute name="project"   default="" />
        <attribute name="args"      default="" />
        <attribute name="wrapper"   default="gradle" />
        <attribute name="dir"       default="${current.dir}" />

        <sequential>
            <gradle task="ear"
                    project="@{project}"
                    args="@{args}"
                    wrapper="@{wrapper}"
                    dir="@{dir}" />
        </sequential>
    </macrodef>


    <!-- Macro to get the generated EAR archive file name -->
    <scriptdef name="gradle.ear.name" language="javascript" description="">
        <attribute name="folder" />
        <![CDATA[
            var files = (new java.io.File(attributes.get("folder"))).listFiles();
            for (i = 0; i < files.length; i++) {
                var fN = files[i].getName();
                if (fn.indexOf(project.name) !== -1 && fn.indexOf(".ear") !== -1) {
                    project.setProperty("ear.name", fn);
                    break;
                }
            }
        ]]>
    </scriptdef>
</project>
